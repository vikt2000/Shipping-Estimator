spring.application.name=shipping-estimator
server.port=8080  # Change the port if needed
spring.datasource.url=jdbc:h2:mem:testdb  # If using H2 for testing
spring.h2.console.enabled=true  # Enable H2 console for testing
